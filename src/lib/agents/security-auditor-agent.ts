// Re-export from the centralized registry (single source of truth)
export { SECURITY_AUDITOR_AGENT_PROMPT } from '@/lib/agents/registry';
export { SECURITY_AUDITOR_AGENT_PROMPT as default } from '@/lib/agents/registry';
