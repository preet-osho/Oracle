// Re-export from the centralized registry (single source of truth)
export { COMPETITOR_INTEL_AGENT_PROMPT } from '@/lib/agents/registry';
export { COMPETITOR_INTEL_AGENT_PROMPT as default } from '@/lib/agents/registry';
